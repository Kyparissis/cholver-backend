"use strict";

/**
 * Function to handle the HTTP PUT request to the /user/{userID}/rate endpoint
 * FR8 - The user must be able to rate other users.
 * @returns resolved promise with the response message
 */
exports.userUserIDRatePUT = function () {
  return new Promise(function (resolve) {
    resolve();
  });
};
